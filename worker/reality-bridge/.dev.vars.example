# 复制为 .dev.vars 供 wrangler dev 本地调试；线上用 wrangler secret put 设置。

# ① 必须与 Sully 应用设置页（pushVapid）生成的 VAPID 公钥/私钥同一对
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# ② 加密 LLM 凭据用主密钥（64 位 hex；与 amsg 同款生成法，可用 openssl rand -hex 32）
MASTER_KEY=

# ③ 可选：客户端调用共享 token（App 设置里填同一值；留空则放行）
CLIENT_TOKEN=
